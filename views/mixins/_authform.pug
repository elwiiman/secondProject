mixin authform(register, error)
  form(class="authform" action=register ? "/signup" : "/login" class="uk-form-stacked" method="post")
      .uk-margin
        label(class="uk-form-label uk-form-label-custom" for="username")=register ? "Username" : "Username or Email"
        .uk-inline
          span.uk-form-icon(uk-icon='icon: user')
          input.uk-input.uk-input-custom.uk-width-medium(type='text' name="username")
      if register    
        .uk-margin
          label(class="uk-form-label uk-form-label-custom" for="email") Email
          .uk-inline
            span.uk-form-icon(uk-icon='icon: mail')
            input.uk-input.uk-input-custom.uk-width-medium(type='email' name="email")

      .uk-margin
        label(class="uk-form-label uk-form-label-custom") Password
        .uk-inline
          span.uk-form-icon(uk-icon='icon: lock')
          input.uk-input.uk-input-custom.uk-width-medium(type='password' name="password")
      if register    
        .uk-margin
          label(class="uk-form-label uk-form-label-custom") Confirm password
          .uk-inline
            span.uk-form-icon(uk-icon='icon: lock')
            input.uk-input.uk-input-custom.uk-width-medium(type='password' name="confirmPass")

          .uk-margin
            label(class="uk-form-label uk-form-label-custom") What do you want to do? 
            .uk-inline   
              select.uk-select.uk-input-custom.uk-width-medium(name="role")
                option(value="") Please select your preference
                option(value="artist") Sell tatooes
                option(value="fan") Buy tatooes

      if error
        .uk-flex.uk-flex-center.uk-alert-danger.uk-width-1-1(uk-alert)
          p.authErr #{error}
      .uk-margin.uk-flex.uk-flex-center   
          button.uk-button.uk-button-primary.button-important(type="submit")=register ? "Register" : "Login"